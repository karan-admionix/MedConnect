<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- ============================================================ -->
        <!-- DAILY DIGEST — Operations Team                               -->
        <!-- ============================================================ -->
        <record id="digest_medconnect_daily" model="digest.digest">
            <field name="name">MedConnect Daily Operations Summary</field>
            <field name="periodicity">daily</field>
            <field name="kpi_res_users_connected">True</field>
            <field name="kpi_mail_message_total">True</field>
        </record>

        <!-- ============================================================ -->
        <!-- WEEKLY DIGEST — Management Team                              -->
        <!-- ============================================================ -->
        <record id="digest_medconnect_weekly" model="digest.digest">
            <field name="name">MedConnect Weekly Performance Report</field>
            <field name="periodicity">weekly</field>
            <field name="kpi_res_users_connected">True</field>
            <field name="kpi_mail_message_total">True</field>
        </record>

    </data>
</odoo>